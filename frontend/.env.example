# 將這些變數複製到 frontend/.env.local（或其他 Next.js 支援的 env 檔）
# 此範例可安全提交到 GitHub。

# 後端 API URL
# - 開發環境：http://localhost:4000
# - 生產環境：留空（使用 Next.js rewrite 代理到 BACKEND_URL）
NEXT_PUBLIC_API_URL=http://localhost:4000

# 後端 URL（僅用於 Next.js rewrite，不暴露給瀏覽器）
# - 生產環境需設定為 Render 後端 URL
# BACKEND_URL=https://your-backend.onrender.com

# Sentry 錯誤追蹤（可選）
# 在 https://sentry.io 創建專案後獲取
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=

# 建議：Do NOT commit secrets into .env.local。僅在開發/測試環境中使用本範例，並替換為實際的部署 URL。