# 將這些變數複製到 frontend/.env.local（或其他 Next.js 支援的 env 檔）
# 此範例可安全提交到 GitHub。

# 後端 API（用來向後端呼叫 REST/GraphQL）
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_API_URL=http://localhost:4000

# 建議：Do NOT commit secrets into .env.local。僅在開發/測試環境中使用本範例，並替換為實際的部署 URL。