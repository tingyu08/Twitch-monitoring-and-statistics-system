# Twitch OAuth 設定
# 請到 https://dev.twitch.tv/console/apps 建立應用程式並取得以下資訊
TWITCH_CLIENT_ID=your_twitch_client_id
TWITCH_CLIENT_SECRET=your_twitch_client_secret
TWITCH_REDIRECT_URI=http://localhost:3000/auth/twitch/callback
TWITCH_VIEWER_REDIRECT_URI=http://localhost:3000/auth/viewer/callback

# TMI.js 聊天機器人設定（用於監聽 Twitch 聊天室）
# 可使用與上方相同的帳號，或專用 Bot 帳號
TWITCH_BOT_USERNAME=your_bot_username
TWITCH_BOT_OAUTH_TOKEN=oauth:your_oauth_token

# EventSub Webhook 設定（用於接收 Twitch 即時事件）
# EVENTSUB_SECRET: 至少 10 字元的隨機字串，用於驗證 Twitch 請求
# EVENTSUB_CALLBACK_URL: 公開的 HTTPS URL（可使用 ngrok）
EVENTSUB_ENABLED=false
EVENTSUB_SECRET=your_eventsub_secret_at_least_10_chars
EVENTSUB_CALLBACK_URL=https://your-ngrok-url.ngrok.io

# JWT 簽章密鑰：請在生產環境使用強隨機字串
# 建議使用 openssl rand -base64 32 生成
APP_JWT_SECRET=your_jwt_secret

# Viewer Token 加密金鑰：32 字元的 base64 字串
# 建議使用 openssl rand -base64 32 生成
VIEWER_TOKEN_ENCRYPTION_KEY=your_viewer_token_encryption_key

# 前端 URL（開發環境預設 localhost）
FRONTEND_URL=http://localhost:3000

# 後端服務端口
PORT=4000

# 環境（development 或 production）
NODE_ENV=development

# Sentry 錯誤追蹤（可選）
# 在 https://sentry.io 創建 Node.js 專案後獲取
SENTRY_DSN=

# SQLite 資料庫路徑（本地開發用）
DATABASE_URL="file:./prisma/dev.db"

# Turso 雲端資料庫（生產環境用）
# 將 DATABASE_URL 改為 Turso URL，程式會自動偵測並切換
# DATABASE_URL=libsql://your-db-name.turso.io
# TURSO_AUTH_TOKEN=your_turso_auth_token

# 使用說明：
# 1. 複製此檔案為 .env，並填入實際值
# 2. 不要將 .env 提交到 GitHub，以免洩漏敏感資訊
# 3. 生產環境請將 DATABASE_URL 設為 libsql:// 開頭的 Turso URL
# 4. 使用 openssl rand -base64 32 生成安全的密鑰
